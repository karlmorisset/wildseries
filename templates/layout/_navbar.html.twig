<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="{{path('app_index')}}"><img src="{{ asset('build/images/favicon.png') }}" alt=""></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">

            {{ render(controller('App\\Controller\\DefaultController::navbarTop')) }}

            <li class="nav-item active">
                <a class="nav-link" href="{{path('program_index')}}">Toutes les séries</a>
            </li>

            <li class="nav-item">
                <a class="nav-link" href="{{path('category_index')}}">Toutes les catégories</a>
            </li>

            <li class="nav-item">
                <a class="nav-link" href="{{path('category_new')}}">Ajouter une catégorie</a>
            </li>

            <li class="nav-item">
                <a class="nav-link" href="{{path('program_new')}}">Ajouter une série</a>
            </li>

            <li class="nav-item">
                <a class="nav-link" href="{{path('season_index')}}">Voir les saisons</a>
            </li>

            <li class="nav-item">
                <a class="nav-link" href="{{path('episode_index')}}">Voir les épisodes</a>
            </li>

            <li class="nav-item">
                <a class="nav-link" href="{{path('actor_index')}}">Voir les acteurs</a>
            </li>

            {% if app.user %}
                <li class="nav-item">
                    <a class="nav-link" href="{{path('user_profile')}}">Mon compte</a>
                </li>
                <li>
                    <a class="nav-link" href="{{path('app_logout')}}">{{ 'account.logout'|trans }}</a>
                </li>
            {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{{path('login')}}">{{ 'account.login'|trans }}</a>
                </li>
                <li>
                    <a class="nav-link" href="{{path('app_register')}}">S'inscrire</a>
                </li>
            {% endif %}

            <li>
                <form action="{{path('app_change_lang')}}" method="GET">
                    <select name="lang" id="lang">
                        <option value="fr" {% if app.request.locale == "fr" %} selected {% endif %}>Français</option>
                        <option value="en" {% if app.request.locale == "en" %} selected {% endif %}>English</option>
                    </select>

                    <button class="btn" type="submit">Choisir la langue</button>
                </form>
            </li>
        </ul>
    </div>
</nav>